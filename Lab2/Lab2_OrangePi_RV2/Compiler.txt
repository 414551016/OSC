usbipd list
usbipd bind --force --busid 5-3
usbipd attach --wsl --busid 5-3

lsblk

ls -al kernel.*
sudo mkdir -p /mnt/sdcard
sudo mount -t ext4 /dev/sdf1 /mnt/sdcard
sudo rm -f /mnt/sdcard/kernel.fit
sudo cp kernel.fit /mnt/sdcard/
ls -al /mnt/sdcard

sudo mkdir -p /mnt/sdcard
sudo mount -t ext4 /dev/sde1 /mnt/sdcard
ls -al /mnt/sdcard
sudo rm -f /mnt/sdcard/kernel.fit
sudo cp kernel.fit /mnt/sdcard/
ls -al /mnt/sdcard

sync
sudo umount /mnt/sdcard/